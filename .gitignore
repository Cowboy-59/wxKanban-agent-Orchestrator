# Dependencies
node_modules/
*/node_modules/

# Build output
dist/
build/

# IDE history / scratch
.history/
.vscode/.history/

# Logs
logs/
*.log
npm-debug.log*

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Kit build artifacts
releases/
kit.tar.gz
kit.zip
*.tgz
*.sha256

# Release-notes scratch. sync-to-orchestrator.mjs --release writes the body here
# for `gh release create --notes-file` and removes it in a finally; a killed run
# leaves it behind, and it must never be committed or published.
.release-notes.tmp.md
