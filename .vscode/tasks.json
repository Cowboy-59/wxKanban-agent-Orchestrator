{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "wxKanban Kit — Install & Connect",
      "detail": "SCOPE-095: on folder open, run the one-shot bootstrap — install deps, register the hosted MCP with Claude Code (.mcp.json), start the gateway, and install the Dev Cockpit. init.mjs is idempotent (PID-guarded gateway, unchanged-.mcp.json short-circuit) so it is safe to run on every open, and it supersedes the standalone gateway-start / cockpit-install tasks so the gateway is never double-started on :3003.",
      "type": "shell",
      "command": "node",
      "args": ["scripts/init.mjs"],
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "focus": false,
        "panel": "dedicated",
        "showReuseMessage": false
      },
      "runOptions": { "runOn": "folderOpen" }
    },
    {
      "label": "Check kit version",
      "type": "shell",
      "command": "node",
      "args": ["scripts/check-kit-version.mjs"],
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "focus": false,
        "panel": "dedicated",
        "showReuseMessage": false
      },
      "runOptions": { "runOn": "folderOpen" }
    }
  ]
}
