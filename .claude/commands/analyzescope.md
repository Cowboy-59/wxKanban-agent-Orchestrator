---
description: Audit one scope doc (holes, ambiguity, internal contradictions) or all scopes (cross-scope conflicts), and persist a findings report.
args: "{{args}}"
ai-compat: universal
claude-code: true
cursor: true
blackboxai: true
---

# analyzescope

> The full methodology for this command is delivered by wxKanban at runtime.
>
> Call the MCP tool `project.get_command_prompt` with `{ "command": "analyzescope" }`, then follow the
> returned instructions exactly, applying the arguments provided with this command:
>
> {{args}}
>
> This requires an active wxKanban subscription. If the fetch returns a subscription error,
> renew at https://wxperts.com/account/billing and retry.
