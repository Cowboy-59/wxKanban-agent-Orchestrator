{
  "name": "@wxkanban/watermark",
  "version": "0.1.0",
  "description": "Shared generated-output watermark (frontmatter + visible footer + zero-width signature) consumed by mcp-server and src/server (scope 082).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "prepare": "tsc -b || echo '@wxkanban/watermark prepare: tsc -b unavailable; assuming dist/ is shipped'"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^4.1.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
