{
  "name": "wxkanban-agent-orchestrator",
  "version": "1.0.4",
  "description": "wxKanban orchestrator kit — MCP + command gateway + rules",
  "type": "module",
  "scripts": {
    "init": "node scripts/init.mjs",
    "health": "node scripts/orchestrator-health-check.mjs",
    "build-release": "node scripts/build-release.mjs"
  },
  "dependencies": {
    "express": "^4.21.2",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "archiver": "^7.0.1"
  }
}
