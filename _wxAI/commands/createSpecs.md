---
description: createSpecs — full spec pipeline (capture → specify → clarify → plan → tasks → lifecycle) via the MCP project.create_specs tool.
args: "{{args}}"
ai-compat: universal
claude-code: true
cursor: true
blackboxai: true
---

# createSpecs — Full Spec Pipeline

> The full methodology for this command is delivered by wxKanban at runtime.
>
> Call the MCP tool `project.get_command_prompt` with `{ "command": "createspecs" }`, then follow the
> returned instructions exactly, applying the arguments provided with this command:
>
> {{args}}
>
> This requires an active wxKanban subscription. If the fetch returns a subscription error,
> renew at https://wxperts.com/account/billing and retry.
